# Swift / SwiftPM
.build/
.swiftpm/
*.xcodeproj
DerivedData/

# Von scripts/build.sh generierter Build-Stempel (Bau-Artefakt, kein Quellcode —
# er wird bei jedem Build aus dem Quellstand neu erzeugt, siehe scripts/build-stamp.sh)
Extension/Sources/RiverSimGD/Generated/

# Godot (später)
game/.godot/
game/bin/*.dylib
game/bin/*.so
export/

# Lokal installierte Werkzeuge (Godot, Swift-Kompatibilitaetsbibliotheken)
.tools/

.DS_Store

# Scratch-Screenshots
scratch_*.png

# Claude-Code-Worktrees (temporär, für parallele Agents)
.claude/worktrees/
